import React from "react";

const TeamProfile = ({ name, position, img }) => {
  return (
    <div>
      <Image src="" width={} height={} />
      <p>{name}</p>
      <p>{position}</p>
    </div>
  );
};

export default TeamProfile;
